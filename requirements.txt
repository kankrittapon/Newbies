# Core dependencies for Newbies Bot (minimal set)
requests>=2.28.0
nuitka>=1.8.0

# Optional - only install if needed
# playwright>=1.30.0  # Only for trial mode
# asyncio-compat>=0.1.2  # Only if Python < 3.7