# Build-time dependencies for compiling with Nuitka

nuitka>=2.4
ordered-set>=4.1.0
zstandard>=0.22.0

# Windows-only helpers
pefile>=2023.2.7; sys_platform == 'win32'
pywin32-ctypes>=0.2.0; sys_platform == 'win32'

# Optional: nicer build logs (not required)
rich>=13.0.0
